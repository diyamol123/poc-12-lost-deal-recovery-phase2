services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "3001:3001"


    depends_on:
      - backend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

    ports:
      - "8001:8000"
